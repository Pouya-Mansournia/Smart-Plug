<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.NMA.pouyamansournia.smart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <activity
            android:name="ir.NMA.pouyamansournia.smart.Activities.Welcome"
            android:theme="@style/AppIntro" />

        <activity android:name="ir.NMA.pouyamansournia.smart.Activities.Setting" />

        <activity android:name="ir.NMA.pouyamansournia.smart.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.SplashST">
            <intent-filter>
                <action android:name="android.intent.action.SplashCT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

///////////////////// NMASMART:159753//////////////////////////
    </application>

</manifest>
